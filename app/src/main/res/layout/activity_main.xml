<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:layout_gravity="center">

    <ImageView
        android:id="@+id/diceimage"
        android:layout_width="242dp"
        android:layout_height="166dp"
        android:src="@drawable/dice_1"
        android:layout_gravity="center"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_margin="20dp"
        android:text="@string/roll"
        android:textSize="20sp"
        android:id="@+id/roll_button"/>


</LinearLayout>

<!--   ********************************************************************************************
Note :
       In this gravity keyword is used for layout gravity is used for the view
       components

       Everytime we define an id for android view a number is create which acts as
       integer constant ( this all happens in R.class and then in kotlin file we
       refer to this id )

   # imageview
      >>their is the imageview class which extends from view
         java.lang.Object
           ↳android.view.View
 	         ↳android.widget.ImageView
 	 >> imageview is commonly used to apply tints to an image and
 	     handle image scaling
 	 >> their is two types of resources of imageview
 	     .Drawables and resources
 	 >> src attribute of the image view is useful to set image view

  #layouts
     are used when we have to put lots of viewgroups



-->